<%= simple_form_for @registration, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Registration</legend>
    
    <%= f.input :section_id, :as => :hidden, :collection => Section.active.alphabetical.all %>
    <%= f.input :student_id, :as => :hidden, :collection => Student.active.alphabetical.all %>
    <%= f.input :final_standing, :collection => Registration::STANDINGS %> 
    <%= f.input :fee_paid %>
    <%# f.input :source, :as => :hidden, :input_html => { :value => "new_page" } %>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', section_path(@registration.section), :class => 'btn' %>
    </div>
  </fieldset>
<% end %>